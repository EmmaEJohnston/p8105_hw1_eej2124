p8105_hw1_eej2124
================
Emma Johnston
2026-09-19

``` r
library(tidyverse)
```

## Problem 1

``` r
data("penguins", package = "palmerpenguins")
```

This data set includes information on the species, island,
bill_length_mm, bill_depth_mm, flipper_length_mm, body_mass_g, sex,
year. It has 8 columns and 344 rows. The mean flipper length is 200.92
mm.

# How do I include “values” of important variables?

# Had to remove NA from df - all good?

### Scatterplot

``` r
ggplot(penguins, aes(x = bill_length_mm, y = flipper_length_mm, color = species)) + geom_point()
```

    ## Warning: Removed 2 rows containing missing values or values outside the scale range
    ## (`geom_point()`).

![](p8105_hw1_eej2124_files/figure-gfm/unnamed-chunk-3-1.png)<!-- -->

# Should I clean up this scatterplot - axis titles, etc?

``` r
ggsave("scatterplot.png")
```

    ## Saving 7 x 5 in image

    ## Warning: Removed 2 rows containing missing values or values outside the scale range
    ## (`geom_point()`).

## Problem 2

``` r
df = 
  tibble(
    vec_numeric = rnorm(10),
    vec_logical = vec_numeric > 0, 
    vec_character = character(length = 10),
    vec_factor = factor(c(length = 10), levels = 3)
  )
```

# Fix vector factor

# Why are means so low?

# Should I set.seed?

The mean of the numeric vector is 0. The mean of the logical vector is
0. The mean of the character vector is NA. The mean of the factor vector
is NA. You cannot take the mean of neither the character nor factor
vectors.

# Write as.numeric code
